import React from "react";
import { Container, Card, TabContainer } from "react-bootstrap";
import { ProjectCards } from "../components";

const Portfolio = () => {
  return (
    <Container>
      <ProjectCards />
    </Container>
  );
};

export default Portfolio;
